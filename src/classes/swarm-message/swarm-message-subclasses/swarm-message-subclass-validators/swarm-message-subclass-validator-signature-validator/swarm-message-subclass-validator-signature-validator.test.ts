import '@types/jest';
import { SwarmMessgeSubclassSignatureValidator } from './swarm-message-subclass-validator-signature-validator';

describe('SwarmMessgeSubclassSignatureValidator tests', () => {
  beforeAll(() => {
    // TODO - start connection with CA
  });
  it('constructor with full options should not throw', () => {});
});
