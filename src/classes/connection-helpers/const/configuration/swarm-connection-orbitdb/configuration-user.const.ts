import { IConnectionBridgeOptionsUser } from 'classes/connection-bridge/types/connection-bridge.types';

export const CONFIGURATION_USER_OPTIONS_DEFAULT: IConnectionBridgeOptionsUser = {
  profile: {},
};
