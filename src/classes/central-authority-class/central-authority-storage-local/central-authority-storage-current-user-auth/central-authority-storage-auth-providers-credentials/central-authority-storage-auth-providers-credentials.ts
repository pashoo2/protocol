/**
 * This is used for storing a credentials
 * used by the user to authorize on an
 * authority provider service. The credentials
 * will be used to automatically authorize
 * the user on each service he was registered
 * before.
 *
 * @export
 * @class CAStorageAuthProvidersCredentials
 */
export class CAStorageAuthProvidersCredentials {}
